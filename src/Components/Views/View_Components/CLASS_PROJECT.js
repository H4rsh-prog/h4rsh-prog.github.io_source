export default class CLASS_PROJECT {
    constructor(project) {
        this.name = project.name;
        this.description = project.description;
        this.technologies = project.technologies;
        this.url = project.link;
    }
}